# Lifeway Maintenance Mode

A standalone bilingual maintenance page for Lifeway English.

The page is intentionally static and contains no application code, credentials,
analytics, tracking scripts, or runtime dependencies. Both `index.html` and
`404.html` are self-contained so that normal and deep-link requests display
the same maintenance notice.

Publishing and custom-domain changes are performed separately through the
repository's GitHub Pages settings.
